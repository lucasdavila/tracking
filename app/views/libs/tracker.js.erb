var data = {
  url: window.location.href,
  datetime: (new Date).toISOString()
};

$.ajax({
  type: 'POST',
  url: '<%= api_visits_url %>',
  data: data,
  xhrFields: {
    withCredentials: true
  }
});
